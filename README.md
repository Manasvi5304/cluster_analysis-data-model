# cluster_analysis-data-model
A Python project demonstrating K-Means clustering for unsupervised data grouping, using the Elbow Method for optimal clusters and visualizing the results with scikit-learn.
